# #2 Styles



### # Displaying Temperature

- 시뮬레이터에서 location을 변경하고 싶으면 debug에 location에 가서 변경해줌.

- props 설치

  ```
  npm install prop-types
  ```

- 코드

  ```javascript
  import React from "react";
  import { View, Text, StyleSheet } from "react-native";
  import PropTypes from "prop-types";
  
  export default function Weather({temp}) {
      return (
          <View style={styles.container}>
              <Text>{temp}</Text>
          </View>
      );
  }
  
  Weather.PropTypes = {
      temp: PropTypes.number.isRequired
  
  };
  
  const styles = StyleSheet.create({
      container: {
          flex: 1,
          justifyContent: "center",
          alignItems: "center"
      }
  });
  
  ```

  ```javascript
  render(){
      const {isLoading, temp} = this.state;
      return isLoading ? <Loading /> : <Weather temp={Math.round(temp)}/>;
    }
  ```

  

### #Getting the Condition Names

- 날씨의 종류 : https://openweathermap.org/weather-conditions

- weather에 condition 추가

  ```javascript
  import React from "react";
  import { View, Text, StyleSheet } from "react-native";
  import PropTypes from "prop-types";
  
  export default function Weather({temp}) {
      return (
          <View style={styles.container}>
              <Text>{temp}</Text>
          </View>
      );
  }
  
  Weather.PropTypes = {
      temp: PropTypes.number.isRequired,
      condition : PropTypes.oneOf([
          "Thunderstorm", 
          "Drizzle", 
          "Rain", 
          "Snow", 
          "Atmosphere", 
          "Clear", 
          "Clouds",
          "Haze",
          "Mist",
          "Smoke	",
          "Dust",
          "Fog",
          "Sand",
          "Ash",
          "Squall",
          "Tornado",
      ]).isRequired
  
  };
  
  const styles = StyleSheet.create({
      container: {
          flex: 1,
          justifyContent: "center",
          alignItems: "center"
      }
  });
  
  ```

  

### # Icons and Styling

- expo/vetcor-icons 라는걸 이미 갖고 있음 

  - 이 라이브러리는 expo init을 통해서 만들어진 템플릿 프로젝트에 디폴트로 인스톨 되어짐.
  - https://expo.github.io/vector-icons/

- 아이콘 추가

  ```javascript
  import * as React from 'react';
  import { Ionicons } from '@expo/vector-icons';
  
  export default class IconExample extends React.Component {
    render() {
      return (
        <Ionicons name="md-checkmark-circle" size={32} color="green" />
      );
    }
  }
  ```

  //공식문서에 있음!

- 화면 배치

  ```javascript
  
  export default function Weather({ temp }) {
      return (
          <View style={styles.container}>
              <View style={styles.halfContainer}>
                  <MaterialCommunityIcons size={96} name="weather-lightning-rainy" />
                  <Text style={styles.temp}>{temp}º</Text>
              </View>
              <View style={styles.halfContainer}>
              </View>
          </View>
      );
  }
  ```

  ```javascript
  const styles = StyleSheet.create({
      container: {
          flex: 1,
          justifyContent: "center",
          alignItems: "center"
      },
      temp:{
          fontSize: 42
  
      },
      halfContainer: {
          flex: 1,
          justifyContent: "center",
          alignItems: "center"
      }
  });
  ```

  

### # Background Gradient

- gradient하기 위해서

  ```
  expo install expo-linear-gradient
  ```

- gradient 예시 사이트: https://uigradients.com/#Frozen



### # Titles and Subtitles



### # Conclusions

- 처음 해보는 리액트 네이티브 프로젝트었다.

- 네이티브 앱 경험밖에 없어서 너무 신세계였다 ! 하나의 소스로 안드로이드, ios에서 다 작동할 수 있다는 것이 ‼️

- 처음이라서 설치할것도 많고, 어렵고.....아무튼 그랬지만 ! 프로젝트를 끝난 시점에서 보니 전체적인 흐름이 이해가 간다.

- 레이아웃 짜는 것도 간단하고! 아무튼 재밌게 많은것을 배우면서 한 프로젝트다 !

- 땡큐 니꼴라스~!(니콜라스는 너무 재밌다)

  ```
  title: "Cold as balls",
  subtitle: "Do you want to build a snowman? Fuck no."
  ```

  